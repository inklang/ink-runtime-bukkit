# Ink Runtime for Bukkit - Status & Implementation details

## Repository Status

 Implementation details

- **Repository:** https://github.com/inklang/ink-runtime-bukkit
- **GitHub Issues:** 4 open
- **Created:** Thu Mar 27, 2026
- **Status:** `incomplete`
- **Implementation:**
  - [x] Core runtime classes (Incomplete)
  - [x] build.gradle.kts (Gradle wrapper)
  - [x] `README.md` and documentation
  - [ ] CHANGE the created ` scripts folder to generate example ` script
  - [x] cleanup the empty directory structure if it doesn't exist)
  - [x] `git rm -rf .gitignore`` if exists)
  - [x]`git rm -rf --cached/.git`
        # Delete repository
  - [x] `rm -rf .gitignore`
  - [x] `rm -rf .gitignore`
  - [x] `rm -rf .gitignore`
  - [x]`rm -rf .gitignore`
  - [x]`rm -rf .gitignore
  # Create scripts directory
  [x] `mkdir -p scripts``